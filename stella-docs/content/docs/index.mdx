---
title: Introduction
description: Stella is a fast, bring-your-own-key, model-agnostic terminal coding agent. Point it at any provider, give it a goal, and let a verifier decide when the work is done.
---

> **A note from the author.** Stella took an enormous amount of work — nights,
> weekends, real API bills, and more shadow-worktree debugging sessions than I
> care to count. I'm genuinely proud of what it became: a coding agent that
> proves its work, respects your machine, and answers to nobody's server but
> your provider's. It is totally free, open source, and dual-licensed
> MIT / Apache 2.0 — I hope you have as much fun using it as I had building it.
>
> — [Mac Anderson](https://github.com/macanderson)

**Stella** is a fast, model-agnostic coding agent that lives in your terminal. It is
**bring-your-own-key (BYOK)**: there is no account and no sign-up — Stella runs on the
provider credentials you already have, and everything it records stays on your machine.

```bash
export ANTHROPIC_API_KEY=your_key_here
stella run "add a --json flag to the export command and update the tests"
```

## What makes Stella different

- **BYOK, no lock-in.** Stella auto-detects the provider from whichever API keys you have
  set. Bring Anthropic, OpenAI, Gemini, Vertex, Bedrock, xAI, DeepSeek, Z.ai, OpenRouter,
  or any OpenAI-compatible local server.
- **Verified outcomes.** [Goal mode](/docs/agent-modes#outcome-driven-goal-mode) doesn't stop on a hunch — a separate
  judge model verifies the definition of done from evidence before the loop ends.
- **Real tools with a permission model.** Read, edit, glob, grep, shell, CI, screenshots,
  issue tracking, and developer-defined script tools — each gated by a per-tool permission
  model and optional [lifecycle hooks](/docs/agent-tools/hooks).
- **Configuration that scales to a team.** Override any provider's base URL, key, or model
  in [`settings.json`](/docs/configuration/settings), merged across a project →
  org-managed → user hierarchy.
- **Local-first telemetry.** Token usage, cost, and per-step metering land in a local
  SQLite store (`.stella/store.db`) on your disk. Nothing is sent anywhere.

## How it fits together

<HeroFlowDiagram />

Inside the stella box: the tool registry (read · edit · bash · grep · ci ·
verify · …), MCP servers from `.stella/mcp.toml`, the memory and code-graph
context engine — and, in goal mode, an independent judge asking "is the goal
*actually* met?".

Stella sends your prompt to the model you selected, lets the model call tools to read and
change your workspace, feeds the results back, and repeats until the task is done. In
[goal mode](/docs/agent-modes#outcome-driven-goal-mode) a second model judges the result from evidence, so the loop
ends on a verified outcome rather than the worker's own say-so.

## The documentation

**Start here**

- **[Getting Started](/docs/getting-started)** — install, initialize a repo, configure
  provider keys.

**Providers &amp; configuration**

- **[API Providers](/docs/api-providers)** — per-provider setup, the model catalog, and
  cross-provider pricing.
- **[Configuration &amp; Settings](/docs/configuration)** — `settings.json`, per-agent
  engine config, credentials, and the scope hierarchy.
- **[Examples &amp; Recipes](/docs/examples)** — complete config files organized by use
  case: coding-plan subscriptions, single-key setups, gateways, local/air-gapped,
  enterprise cloud, team-shared settings, and the three cost-tier profiles.

**Using the agent**

- **[Agent Modes](/docs/agent-modes)** — Command Deck chat, one-shot runs, judged goal
  mode, CI monitoring.
- **[Agent Fleets](/docs/agent-fleets)** — many workers over one task list: a shared
  tree with cooperative claims, or per-task worktree isolation on demand.
- **[Agent Tools](/docs/agent-tools)** — the built-in toolbelt, skills, MCP servers,
  custom commands, hooks, and permissions.
- **[Telemetry &amp; Usage Analytics](/docs/telemetry)** — local-only metering, the
  dashboard, budgets, and the files-touched ledger.

**How it works**

- **[Inference Pipeline](/docs/inference-pipeline)** — triage → recall → plan → scope
  review → witness → execute → verify → judge: how work gets *proven*, not just done.
- **[Context Engine](/docs/context-engine)** — memories, the tree-sitter code graph,
  reflections, and the prompt-cache-native recall that makes them cheap.
- **[Agent Engine Paths](/docs/agent-engine-paths)** — every entry point into the
  engine, mapped.
- **[Principles](/docs/principles)** — the convictions the codebase is built around.

**Reference**

- **[Commands](/docs/commands)** — the full command and flag reference.
- **[Extensions](/docs/extensions)** &amp; **[Scripting](/docs/scripting)** — the event
  bus for embedders, and headless/CI usage.
- **[Donate](/docs/donate)** — keep independent, local-first tooling alive.

**Community**

- **[Showcase](/docs/showcase)** — teams shipping real software with Stella, starting
  with [Oxagen](https://oxagen.sh).
- **[Donate](/docs/donate)** — keep independent, local-first tooling alive.

<Callout type="info">
  Stella is BYOK — it never proxies your traffic through a hosted service. Your keys call
  your provider directly, and your telemetry stays local.
</Callout>
