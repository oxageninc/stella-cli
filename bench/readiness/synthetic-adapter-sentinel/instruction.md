The small Python package in `/workspace/sentinel` has a bug in `slugify`.
Make the existing test suite pass. Keep the public function signature intact
and do not weaken or delete tests. Run it with
`python -m unittest -v checks`.

This is a synthetic readiness task, not a Terminal-Bench task.
